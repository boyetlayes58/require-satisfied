{
  "name": "require-satisfied",
  "version": "1.0.0",
  "description": "Search for \"REQUIRE\" in comments and report if no matching \"SATISFIED\" found",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "Untitled",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.9.1",
    "typescript": "^5.2.2"
  }
}
